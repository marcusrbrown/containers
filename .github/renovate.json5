{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  extends: ['github>marcusrbrown/renovate-config#5.2.0'],
  ignorePaths: [
    '**/node_modules/**',
    '**/bower_components/**',
    '**/vendor/**',
    '**/examples/**',
    '**/__tests__/**',
    '**/test/**',
    '**/tests/**',
    '**/__fixtures__/**',
    '**/out/**',
    '**/dist/**',
    'templates/**',
  ],
  constraints: {
    python: '3.13',
  },
  packageRules: [
    {
      description: ['Disable lockfile maintaenance.'],
      matchUpdateTypes: ['lockFileMaintenance'],
      enabled: false,
    },
    {
      matchDepNames: ['python'],
      allowedVersions: '>=3.13,<3.14',
    },
    {
      description: 'Disable patch updates except for select dependencies.',
      matchUpdateTypes: ['patch'],
      matchPackageNames: ['!typescript', '!python'],
      enabled: false,
    },
    {
      description: 'Do not group TypeScript and Python with other updates',
      matchPackageNames: ['typescript', 'python'],
      groupName: null,
    },
    {
      matchPackageNames: ['aquasecurity/trivy-action'],
      versioning: 'github-releases',
    },
    {
      description: 'Keep poetry.lock in sync for Python dependency updates only.',
      matchManagers: ['poetry'],
      postUpgradeTasks: {
        commands: ['poetry lock'],
        executionMode: 'branch',
      },
    },
    {
      description: 'Keep pnpm-lock.yaml and formatting in sync for npm dependency updates only.',
      matchManagers: ['npm'],
      postUpgradeTasks: {
        commands: ['pnpm install', 'pnpm format'],
        executionMode: 'branch',
      },
    },
  ],
  rebaseWhen: 'behind-base-branch',
}
